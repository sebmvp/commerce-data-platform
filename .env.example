# Warehouse location. Defaults to ./warehouse.duckdb at repo root.
# Override for tests or when running multiple warehouses side-by-side.
CDP_DB=./warehouse.duckdb

# Canonical JSONL source directory. The warehouse is rebuilt from here.
# Defaults to ./sample_data. Point at real data on a production box.
CDP_DATA=./sample_data
